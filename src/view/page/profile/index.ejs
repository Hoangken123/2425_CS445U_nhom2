<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Page</title>
  <!-- Include Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Include FontAwesome for Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <!-- Include Custom CSS -->
  <style>
    /* CSS styles remain the same */
  </style>
  <!-- Include Toastr -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.css">
  <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.6.8/axios.min.js"></script>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-10">
        <div class="card p-4">
          <div class="card-body">
            <div class="text-center">
              <i class="fas fa-user-circle fa-4x mb-3 text-primary"></i>
              <h3 class="profile-header">Hồ Sơ Cá Nhân</h3>
            </div>
            <form id="profile-form">
              <div class="mb-3">
                <label for="ten_hien_thi" class="form-label">Họ và Tên</label>
                <input type="text" class="form-control" id="ten_hien_thi" placeholder="Nhập họ và tên" value="<%= user.ten_hien_thi %>">
              </div>
              <div class="mb-3">
                <label for="email" class="form-label">Email</label>
                <input type="email" class="form-control" id="email" disabled placeholder="Nhập email" value="<%= user.email %>">
              </div>
              <div class="mb-3">
                <label for="so_dien_thoai" class="form-label">Số Điện Thoại</label>
                <input type="text" class="form-control" id="so_dien_thoai" placeholder="Nhập số điện thoại" value="<%= user.so_dien_thoai %>">
              </div>
              <div class="mb-3">
                <label for="level" class="form-label">Quyền:</label>
                <input type="text" class="form-control" id="level" 
                placeholder="Nhập họ và tên" disabled
                value="<%= user.level === 1 ? 'Admin' : 'Nhân viên' %>">
              </div>
              <button type="button" class="btn btn-primary w-100" id="btn-update-profile">Cập Nhật</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </div>
  <!-- Optional JavaScript -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>